<!-- ユーザーの詳細 -->
<div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image(100,100) %>
  <!--ここでもif式が出てきていますが、目的としては
「ログインしているユーザーに本人のみ、編集リンクを表示させる」という状況を作りたいです。
これは、他のユーザーが勝手に自分の情報を編集できてしまう状態では、意図せず他のユーザーの情報を編集してしまったり、なりすましが起こりうる等の問題が発生してしまうためです。

より具体的に言うと、画面に表示しているユーザーと、ログインしているユーザーのIDが一致するか確認する
一致していれば、「プロフィール編集リンク」を表示させるという処理を行うために、if式を使用しています。-->
  <!--if @user == XXXXX「XXXXX」の部分には、現在ログインしているユーザーを取得するために必要な記述が入ります。
何が入るか、考えてみましょう。 (ヒント：devise　ヘルパーメソッド)
-->
  <% if @user == current_user %>
    <p><%= link_to "プロフィール編集", edit_user_path(@user) %></p>
  <% end %>
</div>
<!-- ユーザーの投稿一覧 -->
<!--users/show.html.erbから呼び出す場合には、違うフォルダに部分テンプレートがあるため、フォルダ名も指定する必要があることに気をつけましょう。-->
<%= render 'post_images/list', post_images: @post_images %>